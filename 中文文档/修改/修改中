  问题1：err sig 跳过 + 删除占位记录                                                                                                                                                                                                                                             
                                                                                                                                                                                                                                                                                 
  trade_backfill.py：                                                                                                                                                                                                                                                            
  - _parse_transactions：现在会检测 meta.err，失败的 sig 返回 {"sig": ..., "_error": True, "_reason": ...} 而不是直接丢弃                                                                                                                                                        
  - _fill_placeholders：遇到 _error=True 的记录时，删除对应的 rpc_fill 占位记录，不再留在数据库里                                                                                                                                                                                
                                                                                                 
  问题2：交易类型判断重构（资金流向优先）                                                                                                                                                                                                                                        
                                                                                                                                                                                                                                                                                 
  trade_backfill.py + trade_stream.py 的 _extract_trade_info 统一重写：                                                                                                                                                                                                          
                                                                                                                                                                                                                                                                                 
  1. SOL 净值计算：sol_spent = (preBalances[0] - postBalances[0] - fee) / 1e9，扣除 fee 后的净值                                                                                                                                                                                 
  2. Token 余额遍历：不再只看 mint 对应的第一个余额项，而是遍历 postTokenBalances 找 mint == 监控代币 AND owner == signer 的项，解决新 ATA 创建时 preBalance 为空导致数量为 0 的问题
  3. 交易类型判定：                                                                                                                                                                                                                                                              
    - sol_spent > 0.01 + Token 增加 → BUY                                                                                                                                                                                                                                        
    - sol_spent < 0 + Token 减少 → SELL                                                                                                                                                                                                                                          
    - abs(sol_spent) <= 0.01 + Token 有变动 → TRANSFER                                                                                                                                                                                                                           
    - 兜底：Token 增加 → BUY，Token 减少 → SELL 



下面文件 _handle_message 方法 获取tx_data  结构获取数据不对 导致获取数据不全 ，买卖 判断不出来 。data = json.loads(msg)。msg 结构是下面文件 
/Users/curtdk/.openclaw/workspace/fastapiGmgn/app/services/trade_stream.py
    
    /Users/curtdk/.openclaw/workspace/fastapiGmgn/中文文档/测试说明/tx.md

     # 构建 tx_data 结构，与 _extract_trade_info 期望的格式一致
            tx_data = {
                "_signature": sig,
                "slot": result.get("slot", 0),
                "blockTime": result.get("blockTime"),
                "transaction": result.get("transaction", {}),
                "meta": meta,
            }



当前项目 通过 /Users/curtdk/.openclaw/workspace/fastapiGmgn/app/services/trade_backfill.py 对 获取数据的处理 我检测过都是正确的，这是他获取的数据结构。
但是通过/Users/curtdk/.openclaw/workspace/fastapiGmgn/app/services/trade_stream.py 对数据处理 _extract_trade_info。结果是不对的。这是他的获取的数据结构 
/Users/curtdk/.openclaw/workspace/fastapiGmgn/中文文档/测试说明/kuHelius.md  修复一下 



    async def _handle_message(self, msg: str):
/Users/curtdk/.openclaw/workspace/fastapiGmgn/中文文档/测试说明/kuHelius1.md


优化 TradeStream 类中的 _handle_message 和 _extract_trade_info 方法，以适配 Helius 真实的 transactionNotification 数据结构。这是真实结构。 下面是我通过其他软件总结的需要修复点，你参考一下。
/Users/curtdk/.openclaw/workspace/fastapiGmgn/中文文档/测试说明/kuHelius1.md
修复数据路径：

交易体位于 params['result']。

交易详情是双层嵌套：result['transaction']['transaction']。

适配对象化 AccountKeys：

accountKeys 是对象列表（如 {"pubkey": "...", "signer": true}）。

需修正 signer_index 的获取方式，以正确匹配 preBalances。

完善 Token 校验：

处理 pre/postTokenBalances 时，若 owner 字段缺失，需根据 accountIndex 关联 accountKeys 判定。

优化 SOL 计算：

识别 innerInstructions 中的 Jito 小费地址并从 sol_spent 中扣除，避免支出统计虚高。

DEX 深度扫描：

必须递归扫描 innerInstructions 识别 Pump.fun (6EF8rr...)，因为交易常被嵌套在 Flash 执行器中。


不存在
                "blockTime": result.get("blockTime"),


/Users/curtdk/.openclaw/workspace/fastapiGmgn/app/services/trade_backfill.py
_create_placeholders


下面是要求，你先分析一下告诉我修改计划，可以保持更换后可用。我确认后再修改代码。
_parse_transactions 方法获取tx 详细信息，使用getTransaction 每次一个太慢了，更换为下面方法gettransactionsforaddress 下面是教程，
https://www.helius.dev/docs/api-reference/rpc/http/gettransactionsforaddress


网页端 实时交易列表 sol 字段 还是不显示。后台数据库是有数据的

process_trade 方法去处理 指数计算流程。 由于计算必须从第一个开始算起。所以需要增加条件1.run_full_calculation 计算必须完成。这样之前的数据就算完整了，他的方法里面去掉 await process_trade(db, tx_detail) 的处理。 只让 _handle_message 触发。
2.然后 process_trade 方法里面 需要加上 判断 run_full_calculation 是否完成 然后 按 全量计算 排序 去处理 下一个tx 未处理纪录。具体处理公式也比较复杂，单独建立一个指数计算函数 传入当前纪录 ，具体怎么算后面再说。
 

 

 测发现一个问题 网页点击关闭 按钮的时候 start_consumer 队列没有关闭，队列数据也要清零。点击关闭代表一个新的 mint 的监听，需要关闭和清楚之前内容，包括删除表该笔mint 纪录 Transaction 表 TradeAnalysis表                                 
  ，所有全局变量，指数变量，关闭全部，task 等任务


通过接收trade 首先要先判断是否为庄家，下面是庄家判断流程 ，发给你 你先给出计划，我确定后修改。 
  庄家判定 业务，
 
1. 入口： _calculate_index 
2. 数据表  
TradingUser - 交易用户表：地址、状态（散户/庄家/不确定）
UserCondition- 条件记录表：地址、条件ID、条件分值
 
3. 流程
 
- TradingUser 用户存在并状态 为散户/庄家：直接复用状态 ，如果是庄家 不在发给网页 实时交易列表。只发送 散户 和不确定  
- 用户不存在或为不确定：TradingUser表新建纪录，不确定则重新进入判断方法，新建用户默认状态不确定，然后进入 异步庄家判定
    异步庄家判定状态内容： - 获取交易用户钱包 交易倒序取首笔，转账后销户 满足条件 无资金 加入UserCondition 记录一笔 TradingUser 状态改为庄家 ，其他条件我后面在扩展，案例代码可参考 /Users/curtdk/.openclaw/workspace/fastapiGmgn/test_wallet_tx.py

6. 推送：WS下发 tx_detail.get("sig", "") +最新状态，前端匹配条目更新展示 用户状态 


1.获取庄家状态 需要从 redis 现有 用户状态记录 去获取一下 如果没有 再继续
_calculate_index 方法中。state = await get_trader_state

下面 当第二个mint 来的时候 是不是 清零 计算的
 state["holdingQty"] += buy_qty
        state["holdingCost"] += buy_amount
        state["totalBuyAmount"] += buy_amount
        if state["holdingQty"] > 0:


指数计算和 庄家判断 从这里开始就可以了。
_calculate_index。     _run_dealer_check  判断庄家

不要修改代码。 先给出计划。
我们继续讨论  一个问题。 我需要的逻辑 应该是 如果 
检测没有成功。在下次 可以在检测，超过3次 就不在检查了，这个错误检测 不能影响 主要的庄家检测消息。并 返回信息 给前端，显示有多少出错 正在重复检测的。有多少已经 超过3次 不在检测的。 


我发现 网页 交易列表 里面。记录标记 有些一直是 unknown。原因是 检测没有成功。后面就不在处理了。
我需要 第一次检测失败 → 丢进重试队列，主流程不管了。
重试队列 → 只在主流程空闲时才处理，不抢资源。
最多重试3次 → 3次都失败就放弃，并告诉前端。



signer


当前代码只取第一个找到的代币，如果签名者持有多种代币会出错。 测试中我设置 一个默认mint  和这个 mint 匹配的


_batch_insert
当前项目使用了 Transaction  TradeAnalysis 两个表 记录 sig  和 处理 得到处理的内容 使用的是数据库。 我想使用 改为使用 redis 处理这个数据。 你先给出判断的解决方案和处理方案




_extract_trade_info。方法 获取tx 详细信息。 信息不够充分， 参考 下面文档，把 更详细信息也纪录 保存。包括


讨论下面一个功能，先不要修改代码。 我需要 网页上 有 几个条件可以选择， 1. 使用了地址查找表 (ALT)  直接勾选  2. gas费 小于 0.00001  sol  可以设置 3 . cu 在指定的 范围内 可以设置 范围。 4.风险分 大于多少是庄家 可以设置   选择后 满足条件的 都会检测为庄家。 按照
在 _run_dealer_check 方法中增加处理条件 并在 已经存在的 C001： 之前去处理，   C001： 处理需要helius 调用时间长 把他放在最后面   

不要修改代码，我们先讨论功能：
我需要 在网页端 ，实时交易 列表下面，显示 当前交易的用户列表，用户信息 ，已经在 redis 中，按照非庄家， 当前最大买入卖出 交易排名 在最上面 显示 ，显示 前100个就可以了。

发现问题  先给出问题点 和修改意见确定了 再修改 1. run_full_calculation 方法  rpc_sigs 次序翻了翻了， 第一个是 最新的 应该是第一个最旧的。 处理方案 是不是             await self._batch_insert(all_tx_details)  这个方法可以 不需要，
在  _fetch_all_transactions_before  获取的时候 就直接 存入 redis  就是最新的  最新的 放入最后 就可以了。
2: 网页 钱包 地址 不对。类型 都是 TRANSFER 没有 buy. sell 匹配 。 这里不是 结构改变了， 数据 获取的 不对。检查一下

            # 批次间间隔，避免请求过快
            if interval > 0:
                await asyncio.sleep(interval)

下面需求 ，你先给出 计划。
_run_dealer_check 中 C001：保留。把 C002: ALT 条件（uses_lookup_table） C003: Gas 费条件  C004: CU 条件
 C005: 风险分条件  独立出来 形成一个方法。。 这些判断 不需要 远程api. 和 C001： 分开 
get_trader_state 中 if not state:  下面 增加 上面的方法 ， 进行 是否 庄家 判断。 



async def get_trader_state(redis, mint: str, address: str, sig: str = None) -> dict:
    """
    获取用户状态（从 user:{address} 读取）
    如果 status=unknown 且有 sig，自动入队进行庄家检测
    
    Redis 数据结构：
      user:{address}
        status: "unknown" | "dealer" | "retail"
        conditions: '["C001"]'
        {mint}_holdingQty: "1000"
        {mint}_holdingCost: "5.5"
        {mint}_avgPrice: "0.0055"
        {mint}_totalBuyAmount: "10"
        {mint}_totalSellAmount: "3"
        {mint}_totalSellPrincipal: "2.5"
    """
    if not redis:
        return _default_trader_state()
    
    key = user_key(address)
    state = await redis.hgetall(key)
    
    if not state:
        state = _default_trader_state()
        # 新用户：尝试本地快速判断庄家（C002-C005）
        if sig:
            tx_detail = await tx_redis.get_tx(sig)
            if tx_detail:
                from app.services.dealer_detector import _check_local_dealer_conditions
                is_dealer, conditions = _check_local_dealer_conditions(tx_detail, state)
                state["conditions"] = conditions
                if is_dealer:
                    state["status"] = "dealer"
                    await save_trader_state(redis, mint, address, state)
                    logger.info(f"[庄家判定] {address[:8]}... 新用户本地判断为庄家 (C002-C005)")
                    return state
                logger.info(f"[庄家判定] {address[:8]}... 新用户本地判断为非庄家 (C002-C005)，入队等待 C001")
            # 本地判断不是庄家或无交易详情：入队等待 C001 检测
            await _enqueue_dealer_check(address, mint, sig)
    else:
        # 读取 status 和 conditions
        status = state.get("status", "unknown")
        try:
            conditions = json.loads(state.get("conditions", "[]"))
        except:
            conditions = []
        state["status"] = status
        state["conditions"] = conditions
        
        # 读取 mint 相关字段
        state["holdingQty"] = float(state.get(f"{mint}_holdingQty", "0"))
        state["holdingCost"] = float(state.get(f"{mint}_holdingCost", "0"))
        state["avgPrice"] = float(state.get(f"{mint}_avgPrice", "0"))
        state["totalBuyAmount"] = float(state.get(f"{mint}_totalBuyAmount", "0"))
        state["totalSellAmount"] = float(state.get(f"{mint}_totalSellAmount", "0"))
        state["totalSellPrincipal"] = float(state.get(f"{mint}_totalSellPrincipal", "0"))
        
        # 如果是 unknown 且有 sig，自动入队检测
        if status == "unknown" and sig:
            await _enqueue_dealer_check(address, mint, sig)
    
    return state


通过上面处理    _default_trader_state。方法中。这几个参数 是不是 也不需要了
 "holdingQty": 0.0,
        "holdingCost": 0.0,
        "avgPrice": 0.0,
        "totalBuyAmount": 0.0,
        "totalSellAmount": 0.0,
        "totalSellPrincipal": 0.0,




run_full_calculation 方法中     
# 指数计算（_calculate_index 内部更新 wallet_tag）
            metrics = await _calculate_index(tx_detail, mint)

            # 非庄家交易 → WebSocket 广播
            if not metrics.get("is_dealer"):
                await ws_manager.broadcast(mint, {
                    "type": "trade",
                    "data": {
                        **tx_detail,
                        "wallet_tag": "unknown",
                    }
                })
