
until 参数：
类型：字符串（string）
可选参数：是的，这是一个可选参数
格式：base-58 编码的交易签名
功能：用于指定查询的终止点，当查询到达这个签名时停止搜索（不包含该签名本身）
用途：在获取特定时间范围内的交易时非常有用

before 参数：

类型：字符串（string）
可选参数：是的，这是一个可选参数
格式：base-58 编码的交易签名
功能：分页参数，用于获取指定签名之前（时间上更早）的交易
用途：实现向后分页浏览交易历史
当您使用 before 参数时，返回的交易列表不会包含您指定的那个签名对应的交易
查询只会返回在指定签名之前（时间上更早）发生的交易