通过测试有一下修改 。
1.
_calculate_index
tx_type =TRANSFER  直接break 跳过  不在处理


2.
run_cluster_detection   features = extract_features_from_tx_detail(tx_detail)  已经又了 可是在下面 又提取一遍重复了。包括  manager = create_manager(db)

match_cluster
        features = extract_features_from_tx_detail(tx_detail)

3.
而且我明白了，通过下面 语句
# 先按 signature 索引缩小范围
        candidates = ClusterManager._sig_index.get(features.signature, [])
         直接 就判断了，非常的快捷。等同于  做了 下面判断，
# ── 条件 5：精确内容匹配（transaction_type + 程序ID顺序 + 路由字符串） ──
        if self.settings.match_exact_content_enabled:  
直接就替代了 match_any ，另外 match 里面 提交 1 到 4 我也不需要了，就要这一个 判断就可以了。 帮我简化一下。包括对应的页面admin/cluster-settings  没用的也都删了 我感觉 保留 启用簇组功能 就可以了。




get_user_count 先不用了
candidates = [c for c in candidates if c.get_user_count() > 10]




-------
/admin/trade  页面 簇组信息 不准 ，数据中有 41 个显示 只有29个
簇组统计  指数显示 实时性 ， 簇组改变 用户 改变  用户改变 类型 ，指数改变

        clusters = await manager.get_all_clusters()
get_all_clusters
        cluster = await get_cluster(name)



1.簇组改变 但是 用户 状态没有改变，指数也没有改变。



 


